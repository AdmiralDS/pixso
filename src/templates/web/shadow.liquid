export const {{ name }} = { {% for shadow in shadows %}
  {{ shadow.name }}: '{% for value in shadow.values %}{% if value.isShowInset == "true" %}inset {% endif %}{{ value.x }}px {{ value.y }}px {{ value.radius }}px {{ value.spread }}px rgba({{ value.red }}, {{ value.green }}, {{ value.blue }}, {{ value.alpha }}){% if not forloop.last %},{% endif %}{% endfor %}',{% endfor %}
} as const;